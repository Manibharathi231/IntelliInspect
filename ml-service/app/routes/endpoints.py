from fastapi import APIRouter
# from app.routes.api import  # Use `select` for async queries
# from .api import register

# router = APIRouter()

# router.include_router(register.router, prefix="/register", tags=["user"])

